extends layout.pug

block title
	| webri.ng | #{pageTitle}

block content
	h1 #{contentTitle}
	p
		| You should be redirected automatically. If not, please click 
		a(href='' + redirectLink) here
		| .
