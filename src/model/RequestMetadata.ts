/**
 * Represents the useful metadata associated with a HTTP request to the system.
 * This is used for logging purposes.
 */
export type RequestMetadata = {
	requestIp: string;
	requestId: string;
};
