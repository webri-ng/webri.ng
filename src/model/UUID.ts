/**
 * @type UUID.
 * Placeholder UUID type. Used in place of a concrete implementation for code-clarity
 * purposes.
 * @todo Integrate a more concrete type if a package becomes available.
 */

/**
 * UUID type.
 */
export type UUID = string;
