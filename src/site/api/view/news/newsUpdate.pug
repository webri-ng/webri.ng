extends ../layout.pug

block title
	| webri.ng | #{newsUpdate.title}

block content
	h2= newsUpdate.title
	div.content-block#news-update
		div.date= newsUpdate.dateCreated
		div.content
			| !{newsUpdate.content}
